export const SERVER_INSTRUCTIONS = `Journal MCP connector (v1).

Use create_entry when the user wants to save a journal entry.
Provide a concise title and rewritten_text (polished entry); original_text is optional and stored separately.
Other tools (list, search, update, delete) are not available yet.`;
